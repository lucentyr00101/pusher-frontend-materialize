export const users = () => {
    return `/api/users`
}

export const newUser = () => {
    return `/api/users`
}

export const showUser = id => {
    return `/api/users/${id}`
}

export const updateUser = id => {
    return `/api/users/${id}`
}

export const deleteUser = id => {
    return `/api/users/${id}`
}

export const login = () => {
    return `/oauth/token`
}